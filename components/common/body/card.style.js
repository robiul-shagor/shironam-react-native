import { StyleSheet } from "react-native";
import { TEXTFONT, HEADINGFONT } from "../../../constants";

const styles = StyleSheet.create({
    headingBold: {
        fontFamily: HEADINGFONT.bold
    }
})

export default styles;